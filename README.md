# SmartEnergyMine Backend

An AI + IoT-based backend for optimizing energy use in iron ore comminution.

## Run Locally
```bash
git clone <your_repo_url>
cd SmartEnergyMine
pip install -r requirements.txt
uvicorn app.main:app --reload
```
